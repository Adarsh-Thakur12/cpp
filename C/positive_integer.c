#include <stdio.h>
int main()
{
    int a[10];
    int b[5];
    int s;
    int i;
    printf("Enter any Number:");
        scanf("%d", a[s]);
    if (a[s] > 0)
    {
        printf(" Positive Numbers:%d", b[5]);
    }
    else
    {
    }
    return 0;
}