#include<stdio.h>
#include<math.h>
void main()         //Area of Square using power 
{
 int side;
 printf("Enter the Length of side:");
 scanf("%d",&side);
 printf("The Area of Square is:%f",pow(side,2));
 return 0;
}